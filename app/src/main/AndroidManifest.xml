<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.my119" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".LoginEmployee"/>
        <activity android:name=".LoginEmployer"/>
        <activity android:name=".AssignEmployee"/>
        <activity android:name=".AssignEmployer"/>
        <activity android:name=".MainEmployee"/>
        <activity android:name=".MainEmployer"/>
        <activity android:name=".CheckNotice"/>
        <activity android:name=".CheckAlarm"/>
        <activity android:name=".RegisterNotice"/>
        <activity android:name=".MyPage"/>
        <activity android:name=".Resume"/>
        <activity android:name=".Login" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>